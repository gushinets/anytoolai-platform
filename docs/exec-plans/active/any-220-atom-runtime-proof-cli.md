# Execution Plan: ANY-220 Atom Runtime Proof CLI And Evidence Report

## Status

- State: active
- Owner: agent
- Created: 2026-08-18
- Last updated: 2026-08-21
- Review date: 2026-08-21 (nineteenth round)
- Next action: push `feature/ANY-220` so PR #82 picks up the fixed commit; merge once green.
- Blocker: none.

## Goal

One reproducible command, `python scripts/agent/runner.py atoms-proof`, that drives all 11
standalone atoms and all 3 composite kernel_demo workflows over live HTTP against a running
dev compose stack, verifies the DB ledger/event correlation each run actually left behind,
prints per-case PASS/FAIL evidence plus an 11/11 + 3/3 summary, and persists a privacy-safe JSON
evidence report to `.agent/atoms-proof/`. Dev-only: unlike `dev-smoke`/`prod-smoke`, there is no
`prod-atoms-proof` counterpart -- `runner.py`'s `atoms_proof()` only calls `runtime_identity()`
(the per-worktree dev stack resolver), and `prod_smoke()`'s pattern (an `api_url`-only reach into
the prod compose stack) doesn't extend cleanly here since this command also needs a database URL,
which the prod path has no established resolution mechanism for.

Full design rationale is captured in this file's Decision log below.

## Scope

### In scope

- `scripts/agent/atoms_proof.py`: reuses `kernel_demo_smoke.py`'s HTTP start/poll/schema_ref
  logic verbatim; adds a read-only DB ledger/event check (jobs/action_runs/provider_calls/
  artifacts/event_log tables, filtered by `scenario_session_id`) not covered by either existing
  proof mechanism for a live run.
- `atoms-proof` command wired into `scripts/agent/runner.py`, following `dev-smoke`'s pattern.
- `tests/test_atoms_proof.py`: no-DB/no-network unit tests for the pure classification logic
  and the evidence report writer.
- `scripts/agent/quick_check.py`: added the new test file to `PYTEST_TARGETS`.

### Out of scope

- ANY-221 (live-provider canary) — still runs against the fake provider stack.
- Any change to `kernel_demo_smoke.py`'s own behavior/output, or to the frozen hot-path files
  listed in ANY-220/ANY-24.

## Relevant docs

- `docs/architecture/package-layering.md`

## Contracts touched

- API: none (read-only consumer of existing `/v1/*` endpoints).
- DB: read-only (`jobs`, `action_runs`, `provider_calls`, `artifacts`, `event_log`).
- Config: `atoms-proof`'s `--database-url-env` (name of the env var holding the live-stack DSN;
  `runner.py` sets it to `ANYTOOLAI_ATOMS_PROOF_DATABASE_URL`) and `--database-url-is-percent-encoded`
  (set by `runner.py` since `RuntimeIdentity.database_url` percent-encodes user/password; tells
  `_build_engine`/`create_sync_engine` to decode the database-name segment before connecting).
- Events: none produced; existing event types read and asserted against.
- Frontend: none.

## Implementation steps

- [x] `scripts/agent/atoms_proof.py`: HTTP reuse + DB ledger check (`_classify_ledger` pure
      function + `_check_ledger` DB wrapper), evidence report writer, CLI entrypoint.
- [x] Wire `atoms-proof` into `scripts/agent/runner.py` (function + `COMMANDS` registration).
- [x] `tests/test_atoms_proof.py` (33 cases as of the team-lead-#1 pass, no DB/network) + register
      in `quick_check.py`.
- [x] `validate-configs` / `validate-architecture` / `quick-check` all pass (811 tests as of the
      team-lead-#1 pass; 760 at initial implementation, see Progress log for the count at each
      intermediate pass).
- [x] Live proof: `dev-up` -> `atoms-proof` (11/11 + 3/3, exit 0, evidence JSON written and
      inspected) -> `dev-down`. Ran twice to confirm determinism.
- [x] `full-check` (baseline + frontend typecheck/test/build/generate-api-types + freelancer-suite).
- [x] PR opened (`#82`).

## Validation

- [x] `python scripts/agent/runner.py validate-configs`
- [x] `python scripts/agent/runner.py validate-architecture`
- [x] `python scripts/agent/runner.py quick-check`
- [x] `python scripts/agent/runner.py dev-up && python scripts/agent/runner.py atoms-proof && python scripts/agent/runner.py dev-down`
- [x] `python scripts/agent/runner.py full-check`

## Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-08-18 | Filter `action_runs`/`provider_calls`/`artifacts` by `scenario_session_id`, not `job_id` | `CaseResult` from the reused `kernel_demo_smoke._run_one_case` only exposes `session_id`; `job_id` isn't available without an extra DB lookup, and all three tables already carry an indexed `scenario_session_id` column. |
| 2026-08-18 | Resolve `job_id`/`result_artifact_id` via one `jobs_table` lookup keyed by `scenario_session_id` | Needed for the `result_artifact_id` membership check; avoids modifying `kernel_demo_smoke.py` (out of scope) or adding an extra HTTP round-trip. |
| 2026-08-18 | Coerce `RuntimeIdentity.database_url`'s bare `postgresql://` to `postgresql+psycopg://` before `sa.create_engine` | Repo only installs `psycopg` v3, no `psycopg2`; the bare scheme defaults to the psycopg2 dialect and fails at engine-creation time otherwise. |
| 2026-08-18 | Check `result_artifact_id` is present among artifacts (membership), not "equals the last step's artifact id" | `workflows/runner.py`'s `_create_final_artifact` always creates a separate artifact row (`action_run_id=None`); it never reuses a step's own `output_artifact_id` (confirmed by `test_composite_workflow_matrix.py`). |
| 2026-08-18 | Split `_check_ledger` (DB query) from `_classify_ledger` (pure pass/fail logic) | Makes the PROOF00x failure-classification branches unit-testable with fake dict rows, no real Postgres needed. |
| 2026-08-20 | Moved `_atom_coverage_error`/`_composite_coverage_error` checks from the tail of `run()` into `main()`, before any live case runs | First code review pass found `run()` only checked atom coverage, and only after burning all 14 live HTTP/DB cases; matches `kernel_demo_smoke.py`'s `main()` contract exactly. |
| 2026-08-20 | Split `run()`'s per-group loop into `_run_case_group()`, chaining the degraded `case_timeout` from the atom group into the composite group | Composite cases were always using the full `timeout`, wasting ~75s when the atom group already detected a stuck worker; now matches `kernel_demo_smoke.py`'s `_run_case_batch` chaining. |
| 2026-08-20 | Monkeypatch `tests.test_kernel_demo_smoke.load_smoke_module` for the duration of loading `test_atom_runtime_matrix.py` | That test file's own module-level `_SMOKE_MODULE = load_smoke_module()` was re-execing `kernel_demo_smoke.py` a second time and clobbering `sys.modules['kernel_demo_smoke_module']`; pointing it at the already-loaded `smoke` avoids the duplicate parse. |
| 2026-08-20 | Extracted `collect_context.write_timestamped_json_bundle()`; both `write_bundle()` and `write_evidence_report()` now call it | `write_evidence_report()` was reimplementing the same timestamped-JSON-bundle convention independently, risking future drift between the two. |
| 2026-08-20 | Restored an empty-case guard inside `run()` (`PROOF008`/`PROOF009`) after moving the coverage gate into `main()` | Second code review pass found `run()`'s own docstring documents it as callable directly, bypassing `main()`'s gate; without its own guard an empty `ATOM_SMOKE_CASES`/`COMPOSITE_SMOKE_CASES` read as vacuous `0/0` success. Same two-guard structure as `kernel_demo_smoke.py`'s `run()`. |
| 2026-08-20 | Added regression tests for all 3 first-pass behavior fixes plus the new empty-case guard | Second code review pass found none of the three first-pass fixes (coverage gate in `main()`, `case_timeout` chaining, `load_smoke_module` dedup) had test coverage; `kernel_demo_smoke.py` has exactly this regression coverage already. |
| 2026-08-20 | Extracted `kernel_demo_smoke._next_case_timeout()`; both its own `_run_case_batch()` and `atoms_proof.py`'s `_run_case_group()` now call it, and `atoms_proof.py`'s own `DEGRADED_TIMEOUT_SECONDS` copy was dropped in favor of `smoke.DEGRADED_TIMEOUT_SECONDS` | Third code review pass found the degraded-timeout chaining algorithm re-implemented from scratch in `atoms_proof.py` instead of shared, the same class of duplication already fixed once for the evidence-report writer. |
| 2026-08-20 | `write_timestamped_json_bundle()` takes an optional `timestamp` kwarg; `write_bundle()` captures it before calling `collect()` | Third code review pass found the second-pass refactor had silently moved the timestamp to *after* `collect()` (which can take ~20s via `docker compose logs/ps`), changing what the filename means. |
| 2026-08-20 | `write_evidence_report()` takes `exit_code` as an explicit argument and derives `all_passed` from it (`exit_code == 0`) instead of re-deriving `len(passed) == len(cases)` | Fifth code review pass found a direct `run()` call with an empty `ATOM_SMOKE_CASES`/`COMPOSITE_SMOKE_CASES` (PROOF008/PROOF009, `exit_code=1`) still produced `"all_passed": true` in the evidence JSON. |
| 2026-08-20 | `run()`'s engine usage wrapped in one `try`/`finally` calling `engine.dispose()` once | Fifth code review pass found `dispose()` was only called on the two explicit return paths, leaking the SQLAlchemy engine on any exception raised inside case-running. |
| 2026-08-20 | Extracted `kernel_demo_smoke._coverage_gate_error()`; both scripts' `main()`s call it instead of two literally duplicated atom+composite coverage checks | Fifth code review pass found the coverage-gate block was copy-pasted verbatim into `atoms_proof.py`'s `main()`, despite this PR having already extracted two other shared conventions to stop exactly this kind of drift. |
| 2026-08-20 | `tests/test_kernel_demo_smoke.py`'s `load_smoke_module()` (and `tests/test_atoms_proof.py`'s `load_atoms_proof_module()`) now cache their loaded module instead of re-executing on every call | Fifth code review pass found the third-pass `load_smoke_module` monkeypatch was non-reentrant and re-applied on every one of 20 test functions; caching at the source removes the monkeypatch entirely (no longer needed) and fixes the same 20x-reload waste in `load_atoms_proof_module()`. |
| 2026-08-20 | Extracted `tests/module_loading.py`'s `load_cached_module(name, path)`; both `load_smoke_module()` and `load_atoms_proof_module()` became thin wrappers over it | Sixth code review pass found the two caching blocks added by the fifth-pass fix were themselves near-identical duplicates of each other. |
| 2026-08-20 | Added regression tests for the atom-half of `_coverage_gate_error()` in `kernel_demo_smoke.py`'s own `main()`, and for `write_bundle()`'s timestamp-before-`collect()` ordering | Sixth code review pass found `kernel_demo_smoke.py`'s own atom-coverage-mismatch path had no test (only `atoms_proof.py`'s did), and no test could distinguish timestamp-before-`collect()` from timestamp-after (the existing `write_bundle` test stubs `collect()` as an instant lambda). |
| 2026-08-20 | Declined 5 of the sixth pass's 8 findings (degraded-timeout inheritance across the atom/composite boundary, `run()`'s guards being unreachable via `main()`, further loop-body unification beyond the shared timeout formula, `atoms_proof.py` depending on `tests.test_kernel_demo_smoke`, PROOF009 firing after the atom batch) | Each mirrors an existing, already-reviewed design decision in `kernel_demo_smoke.py` or this plan's own stated design, rather than a new defect. |
| 2026-08-20 | Removed `atoms_proof.py`'s own `_load_module_from_path()`; its one call site (loading `test_atom_runtime_matrix.py`) now calls `tests.module_loading.load_cached_module()` | Seventh code review pass found it re-implemented the exact same sys.modules-registration-plus-cache technique the sixth pass had just extracted into `tests/module_loading.py` for the other two loaders. |
| 2026-08-20 | `run()`'s docstring now says `main()` calls `smoke._coverage_gate_error()`; the `_test_kernel_demo_smoke_module` import alias replaced with a direct `from tests.test_kernel_demo_smoke import load_smoke_module` | Eighth code review pass found the docstring still described the pre-fifth-pass two-call coverage contract, and the alias was used exactly once. |
| 2026-08-20 | `tests/module_loading.py`'s manual `_CACHE: dict[str, ModuleType]` (keyed only on `name`) replaced with `@functools.lru_cache(maxsize=None)` over `load_cached_module(name, path)`, keyed on the full `(name, path)` pair | Ninth code review pass found the name-only cache key could silently return the wrong module if a future call ever reused a `name` with a different `path`; `lru_cache` fixes that and replaces 12 lines of manual bookkeeping with one decorator line. |
| 2026-08-20 | `kernel_demo_smoke.py`'s own `run()` docstring updated to name `_coverage_gate_error()` instead of the old two-call contract | Tenth code review pass found the eighth pass had fixed this exact drift in `atoms_proof.py`'s mirrored docstring but missed the module where `_coverage_gate_error()` and the `main()` call site it describes actually live. |
| 2026-08-20 | `main()` now writes an evidence report (empty cases, exit_code=1) and prints its path even when the coverage gate fails, not only on a live run | Eleventh code review pass found a coverage-gate failure -- one of the ticket's required non-zero-exit categories -- left no evidence artifact behind, defeating the script's whole stated purpose for that failure mode. |
| 2026-08-20 | `_run_case_group()` derives `passed` from `results` after the loop instead of an independently incremented counter; `tests/test_collect_context.py`'s loader now wraps `tests.module_loading.load_cached_module()`; `test_run_chains_degraded_timeout_from_atom_batch_into_composite_batch()` reuses `_fake_case_result()` from `tests/test_kernel_demo_smoke.py`; a shared `assert_main_fails_on_coverage_mismatch()` replaced 4 near-identical coverage-gate test bodies across both test files | Eleventh code review pass found each as its own instance of the same duplication class this review chain has repeatedly fixed (two sources of truth for one fact; a third structurally identical loader left un-migrated; copy-pasted fakes/test bodies). |
| 2026-08-20 | Declined: renaming `_coverage_gate_error`/`_next_case_timeout`/`_TIMEOUT_ERROR_CODE` to non-underscore names | Eleventh code review pass raised it, but `atoms_proof.py` already reaches into `kernel_demo_smoke.py`'s underscore-prefixed `_run_one_case`/`_TIMEOUT_ERROR_CODE` from before this review chain started -- an established, consistent convention (the two scripts are co-maintained, not a public library boundary), not a new problem this diff introduced. |
| 2026-08-20 | Removed every "`/code-review`" phrasing (a local skill/slash-command name, meaningless to a reader without it) from this doc, `atoms_proof.py`, `kernel_demo_smoke.py`, and the test files, in favor of plain "code review"; removed every reference to `plans/ANY-220.md` from tracked files/comments (it's gitignored -- a reference to it is a dead link for anyone else's checkout) | User feedback: skill-specific names and links to gitignored files don't survive outside this session/environment. |
| 2026-08-20 | Extracted `kernel_demo_smoke._empty_cases_error()`; both scripts' `run()`s call it instead of two structurally identical empty-tuple guards | Twelfth code review pass found the vacuous-success guards (SMOKE007/SMOKE010 vs PROOF008/PROOF009) were the one thing this PR's dedup pass hadn't yet unified, unlike `_next_case_timeout()`/`_coverage_gate_error()`/`write_timestamped_json_bundle()`. |
| 2026-08-20 | Extracted `collect_context.TIMESTAMP_FORMAT`; both `write_timestamped_json_bundle()`'s default and `write_bundle()`'s own timestamp calculation reference it | Twelfth code review pass found the UTC timestamp format string `"%Y%m%dT%H%M%SZ"` was duplicated once in the same file. |
| 2026-08-20 | `write_evidence_report()` asserts `exit_code == 0` never coincides with a failing case in `cases` | Twelfth code review pass found nothing caught a caller passing mismatched `exit_code`/`cases`, which would make the payload's own `all_passed: true` contradict its own per-case detail. |
| 2026-08-20 | `tests/module_loading.py`'s `@functools.lru_cache(maxsize=None)` replaced with `@functools.cache` | Twelfth code review pass found the repo requires Python >=3.12, where `@functools.cache` is the more direct spelling of the same unbounded cache. |
| 2026-08-20 | `write_evidence_report()`'s `exit_code`/`cases` consistency check changed from a bare `assert` to `if ...: raise ValueError(...)` | Thirteenth code review pass found a bare `assert` is compiled out entirely under `python -O`/`PYTHONOPTIMIZE`, silently disabling the invariant it exists to enforce. |
| 2026-08-20 | `main()`'s coverage-gate-failure branch now sets `cases, exit_code = [], 1` instead of returning early; both it and the normal `run()` path converge on one `write_evidence_report()`/print/`return exit_code` | Thirteenth code review pass found the last remaining instance of a duplication class this PR fixed repeatedly elsewhere -- the same two-line "write report, print path" pattern was copy-pasted across both branches. |
| 2026-08-20 | `atoms_proof.py`'s CLI takes `--database-url-env ENV_VAR` instead of a `database_url` positional; `runner.py`'s `atoms_proof()` sets that env var via `run_with_env()` instead of passing the URL on argv | Fourteenth-round inline review comment (verified, not assumed): `RuntimeIdentity.database_url` can embed a real `ANYTOOLAI_POSTGRES_PASSWORD` override, which an argv value would expose via `ps`/process listings; `dev_smoke()` (the pattern `atoms_proof()` was modeled on) never had this problem since it only passes a non-secret `api_url`. |
| 2026-08-20 | Declined: counting `provider.request_started`/`provider.request_succeeded` events in `_classify_ledger()` | Fourteenth-round inline review comment raised it, but `PROOF003` already checks "exactly one `provider_calls` row per `action_run`" directly against the table (a stronger signal than event-log counting), and neither `tests/db_support.assert_scenario_runtime_correlation` nor `test_composite_workflow_matrix.py` -- both explicitly named as this ticket's DB-check reference pattern -- do this count either. |
| 2026-08-20 | Partially reversed: `_check_ledger`'s `except sa.exc.SQLAlchemyError` branch now writes `type(exc).__name__` instead of `str(exc)` into the persisted `error_message`; `kernel_demo_smoke.CaseResult`-sourced messages (SMOKE001/002/004/009) left unchanged | CodeRabbit pushed back on the decline below: `database_url` is no longer assumed dev-only (the same premise the argv-to-env fix in this round already relied on), so a raw `SQLAlchemyError` string persisted into evidence JSON was a real, if narrow, gap. The HTTP-layer messages weren't re-litigated -- they embed only `api_url` (non-secret, already printed on every `dev-up`) and exception text from an unauthenticated flow with nothing to leak; `SMOKE004` was already verified safe via `ScenarioSessionResponse`'s `extra="forbid"` schema. Added `test_check_ledger_reports_proof000_without_leaking_raw_exception_text` -- the first test coverage this exception branch had at all. |
| 2026-08-20 | Declined: replacing `EvidenceCase.error_message`/`_check_ledger`'s `SQLAlchemyError` text with an allowlisted message | Fourteenth-round inline review comment raised it; verified `ScenarioSessionResponse` (`apps/platform-api/.../schemas.py`, `extra="forbid"`) exposes only ids/enum/label fields even on a "failed" session (no prompt/user-input/provider content). The DB-exception half of this decline was subsequently reversed (see the row above) once the "dev-only DSN" premise was shown to be inconsistent with this round's own argv-to-env fix; the HTTP-layer-message half stands. |
| 2026-08-20 | Reversed: the HTTP-layer-message half of the row above. `_run_case_with_ledger_check` now prints `CaseResult.error_message` to stderr once and stores only `f"{error_code}: HTTP-layer case failed for scenario_id {scenario_id} (see stderr for full diagnostic)"` in `EvidenceCase` | Team-lead-#1 review (request-changes) found `SMOKE004` embeds the *entire* raw scenario-session response body verbatim, not just an id -- whatever fields the API happens to return, with no allowlist enforcing `EvidenceCase`'s own "ids/labels/booleans only" docstring claim on that path. Schema-safety of one endpoint's response model doesn't generalize to every `CaseResult.error_message`-producing failure path (timeouts, connection errors, other endpoints), so the raw text is no longer persisted at all. |
| 2026-08-20 | Reversed: row 119's decline. Added `PROOF013` (`provider.request_started`/`provider.request_succeeded` correlated per `provider_call_id`, exactly one of each), and upgraded `PROOF006` from an aggregate count to per-`action_run_id` correlation | Team-lead-#1 review found `PROOF003` and event-log completeness check different tables/scopes (`provider_calls` row identity vs. `event_log` row identity) and are not redundant -- a two-step workflow with correct `provider_calls` rows but misattributed/duplicated `event_log` rows could still report PASS under the old aggregate-count check. The provider-gateway contract treats `event_log` as the domain source of truth, so this gap was real. |
| 2026-08-20 | Added `PROOF011` (each `action_run.job_id` must equal the resolved `job_row["id"]`) and `PROOF012` (every `provider_calls` row's `action_run_id` must belong to a fetched `action_run`, catching orphans) | Team-lead-#1 review: `_check_ledger`'s 5 queries each filter independently by `scenario_session_id` with no cross-table join, so a mislinked `action_run.job_id` or an orphan `provider_calls` row (right `scenario_session_id`, wrong `action_run_id`) would previously pass silently. |
| 2026-08-20 | `RuntimeIdentity.database_url` now percent-encodes `ANYTOOLAI_POSTGRES_USER`/`ANYTOOLAI_POSTGRES_PASSWORD` via stdlib `urllib.parse.quote`, not raw f-string interpolation | Team-lead-#1 review: a password containing a reserved URL character (`@`, `:`, `/`, `%`, `#`) previously parsed into the wrong host/user, breaking `atoms-proof` against an otherwise-healthy stack -- the exact class of bug `storage/db.py`'s `build_postgres_url_from_env` already exists to prevent for the app's own internal DSN. Used stdlib `quote()` rather than importing `sqlalchemy` into this otherwise dependency-light bootstrap script. |

## Progress log

| Date | Progress | Next |
|---|---|---|
| 2026-08-18 | Implemented `atoms_proof.py`, wired `atoms-proof`, added tests, ran quick-check gate and a live `dev-up`/`atoms-proof`/`dev-down` cycle twice — both 11/11 + 3/3, exit 0. Ran `full-check` (frontend + backend) clean. | Open PR. |
| 2026-08-20 | Merged `main` into `feature/ANY-220`, opened PR #82, filled in the PR body. First code review pass found 6 issues in `atoms_proof.py` (coverage checks not gating before live runs, missing degraded-timeout chaining into the composite batch, a redundant `kernel_demo_smoke.py` reload, evidence-report/`collect_context.py` duplication, dead `SCRIPT_DIR`). Fixed all 6; re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (785 passed), and a live `dev-up`/`atoms-proof`/`dev-down` cycle (11/11 + 3/3, exit 0). | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Second code review pass on that fix found 2 issues: `run()` lost its own empty-case guard when the coverage gate moved to `main()` (vacuous `0/0` success on direct `run()` calls), and none of the 3 first-pass behavior fixes had test coverage. Fixed both: restored the guard (`PROOF008`/`PROOF009`) and added 5 regression tests. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (790 passed), and a live `dev-up`/`atoms-proof`/`dev-down` cycle (11/11 + 3/3, exit 0). | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Third code review pass found no correctness bugs but 5 reuse/cleanliness findings: dead `import json`, `write_bundle()`'s timestamp now captured after `collect()` instead of before, `_run_case_group()` re-implementing `_run_case_batch()`'s timeout-chaining algorithm from scratch, an undocumented non-reentrant monkeypatch assumption, and a duplicated `DEGRADED_TIMEOUT_SECONDS` constant. Fixed all 5 (shared `_next_case_timeout()`, `timestamp` kwarg on `write_timestamped_json_bundle()`, dead import removed, `ponytail:` comment added). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (790 passed, unchanged — pure refactor), and a live `dev-up`/`atoms-proof` + `dev-smoke` + `collect-context`/`dev-down` cycle (all green, confirming the shared-helper refactor didn't regress the sibling module). | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Fourth code review pass verified all 5 third-pass fixes; no findings survived. Fifth code review pass (8 finder agents, 3 correctness + 3 cleanup angles + altitude + conventions, ~15 raw candidates deduped to 6 verified findings) found: `write_evidence_report()`'s `all_passed` field could read `true` on a non-zero `exit_code` for a direct `run()` call against an empty case list; `run()`'s `engine.dispose()` wasn't wrapped in `try`/`finally` and could leak on an exception; `main()`'s coverage-gate block was duplicated verbatim between the two proof scripts instead of shared; the atom half of that gate had no regression test (only the composite half did); and the third-pass `load_smoke_module` monkeypatch was non-reentrant *and* re-applied on every one of 20 test functions. Fixed all 6: threaded `exit_code` into `write_evidence_report()`, wrapped `run()`'s engine usage in one `try`/`finally`, extracted `kernel_demo_smoke._coverage_gate_error()` shared by both `main()`s, added the missing atom-coverage-gate test, and — the deeper fix for the monkeypatch finding — made `load_smoke_module()`/`load_atoms_proof_module()` cache their loaded module, which also let the whole monkeypatch block in `atoms_proof.py` be deleted outright. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (794 passed, +4 new tests), and a live `dev-up`/`atoms-proof` (`all_passed: true` in the evidence JSON) + `dev-smoke` + `collect-context`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Sixth code review pass found no correctness bugs but 8 findings; 3 fixed (missing atom-half regression test for `kernel_demo_smoke.py`'s own coverage gate, missing regression test for `write_bundle()`'s timestamp-before-`collect()` ordering, and the two near-identical fifth-pass caching blocks deduplicated into `tests/module_loading.py`'s `load_cached_module()`), 5 declined as already-reviewed intentional design rather than defects. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (796 passed, +2 new tests), and a live `dev-up`/`atoms-proof` + `dev-smoke` + `collect-context`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Seventh code review pass found no correctness bugs and only 1 minor finding: `atoms_proof.py`'s own `_load_module_from_path()` duplicated the sys.modules-registration-plus-cache technique the sixth pass had just extracted. Fixed by removing it and calling `tests.module_loading.load_cached_module()` at its one call site instead. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (796 passed, unchanged -- pure refactor), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Eighth code review pass found no correctness bugs and 2 minor findings: `run()`'s docstring still described the pre-fifth-pass two-call coverage contract, and an unnecessary `_test_kernel_demo_smoke_module` import alias was used exactly once. Fixed both. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (796 passed, unchanged -- pure cleanup), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Ninth code review pass found no correctness bugs and 2 minor findings, both in the new `tests/module_loading.py`: its manual cache was keyed only on `name` (silently wrong-module risk if a future call reused a `name` with a different `path`) and hand-rolled what `functools.lru_cache` already provides. Fixed by switching to `@functools.lru_cache(maxsize=None)` keyed on `(name, path)`. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (796 passed, unchanged -- pure refactor), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Tenth code review pass found no correctness bugs and 1 minor finding: `kernel_demo_smoke.py`'s own `run()` docstring still described the old two-call coverage contract -- the eighth pass had fixed the same drift in `atoms_proof.py`'s mirrored docstring but not here, in the module that actually owns `_coverage_gate_error()`. Fixed. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (796 passed, unchanged -- pure docstring fix), and a live `dev-up`/`atoms-proof`/`dev-smoke`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Eleventh code review pass found 6 findings; fixed 5 (coverage-gate failure now writes an evidence artifact, `_run_case_group()`'s duplicate `passed` counter removed, `tests/test_collect_context.py`'s loader migrated to the shared `load_cached_module()`, a fake-result duplicate replaced with the existing `_fake_case_result()` import, 4 near-identical coverage-gate tests unified into one shared `assert_main_fails_on_coverage_mismatch()` helper), declined 1 (renaming underscore-prefixed cross-module members -- matches this file's pre-existing convention, not a new problem). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (798 passed, +2 new tests), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | User feedback (not a code-review pass): removed all "`/code-review`" phrasing (a local skill name unknown outside this environment) in favor of plain "code review", and removed all references to the gitignored `plans/ANY-220.md` from tracked files/comments, replacing them with either an inline summary or a pointer to this doc's own Decision log where the same content already lives. Touched this doc, `scripts/agent/atoms_proof.py`, and 3 test files. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (798 passed, unchanged -- pure docs/comments), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Twelfth code review pass found no correctness bugs and 4 cleanup findings: the empty-case guards were the last un-unified duplication class (unlike `_next_case_timeout()`/`_coverage_gate_error()`/`write_timestamped_json_bundle()`), a duplicated timestamp format string, a missing defensive check for `write_evidence_report()`'s `exit_code`/`cases` consistency, and `lru_cache(maxsize=None)` where `@functools.cache` is more direct. Fixed all 4 (`_empty_cases_error()`, `TIMESTAMP_FORMAT`, an assert, `@functools.cache`). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (801 passed, +3 new tests), and a live `dev-up`/`atoms-proof`/`dev-smoke`/`collect-context`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Thirteenth code review pass (tail of a 12-pass chain, 7 of 8 finder angles empty) found 2 findings: the twelfth pass's new `assert` in `write_evidence_report()` is compiled out under `-O`/`PYTHONOPTIMIZE`, and `main()` still duplicated the "write report, print path" pattern across its two branches. Fixed both (`raise ValueError` instead of `assert`; both branches now converge on one `write_evidence_report()` call). Two candidates declined as already-settled in earlier decision-log rows (further test-helper parameterization; unifying `_run_case_group`/`_run_case_batch`). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (801 passed, unchanged -- pure refactor), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Fourteenth round: user-supplied inline review comments (explicitly flagged as untrusted data to verify, not follow blindly). Verified each against current code/schema; 4 of 6 confirmed and fixed (stale `plans/ANY-219.md`/`/code-review` bullet removed from the ANY-219 exec-plan doc; this doc's Implementation-steps test count and "PR opened" checkbox brought current, plus the "dev/prod" Goal-section overclaim narrowed to "dev" since there's no `prod-atoms-proof`; `database_url` moved from `atoms_proof.py`'s argv to an env-var reference so a real `ANYTOOLAI_POSTGRES_PASSWORD` override can't leak via `ps`), 2 declined after verification (provider-event counting -- redundant with `PROOF003`'s direct table check and absent from the ticket's own named DB-check reference patterns; allowlisting `EvidenceCase.error_message` -- the claimed leak doesn't exist, `ScenarioSessionResponse` is `extra="forbid"`-locked to ids/enums even on failure). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (804 passed, +3 new tests), and a live `dev-up`/`atoms-proof`/`dev-down` cycle -- confirmed the printed subprocess command line no longer contains the database URL. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | CodeRabbit pushed back (PR comment, not a full review pass) on the fourteenth round's `EvidenceCase.error_message` decline, specifically the `_check_ledger` `SQLAlchemyError` half; verified the pushback's core point (the "dev-only DSN" premise contradicted the same round's own argv-to-env fix) and partially reversed: `_check_ledger`'s exception message now uses `type(exc).__name__` instead of `str(exc)`. The HTTP-layer-message half of the original decline stands (re-verified, not re-litigated). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (805 passed, +1 new test -- `_check_ledger`'s exception branch had zero prior coverage), and a live `dev-up`/`atoms-proof`/`dev-down` cycle, all green. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Team-lead-#1 review (request-changes, 5 verified findings) treated as untrusted data and re-verified against current code before fixing. Confirmed and fixed all 5: (1) `PROOF013` added for per-`provider_call_id` event correlation, `PROOF006` upgraded from an aggregate count to per-`action_run_id` correlation -- reverses row 119's earlier decline, which had incorrectly assumed `PROOF003`'s `provider_calls`-table check made an `event_log` check redundant; (2) `_run_case_with_ledger_check` no longer persists raw `CaseResult.error_message` (`SMOKE004` embeds the full raw session response body) -- prints it to stderr once, persists only an allowlisted `f"{error_code}: ... (see stderr for full diagnostic)"`, reversing the HTTP-layer half of row 121's decline; (3) `RuntimeIdentity.database_url` now percent-encodes credentials via stdlib `urllib.parse.quote`; (4) added `PROOF011` (`action_run.job_id` ownership) and `PROOF012` (orphan `provider_calls` rows) -- the 5 `_check_ledger` queries filter independently by `scenario_session_id` with no cross-table join; (5) added `test_check_ledger_wires_all_five_query_results_into_classify_ledger` (fake-connection test) and `test_run_case_with_ledger_check_dispatches_a_passing_http_result_into_check_ledger`. Also fixed the stale test-count checklist entries. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (811 passed, +6 new tests), and a live `dev-up`/`atoms-proof`/`dev-down` cycle. | Push the fixed commit and merge PR #82. |

| 2026-08-20 | Fifteenth round: added `PROOF014`/`PROOF015` (orphan `event_log` rows for `action.started`/`action.succeeded` and `provider.request_started`/`provider.request_succeeded`, mirroring `PROOF012` for the event-log side) and percent-encoded `ANYTOOLAI_POSTGRES_DB` in `RuntimeIdentity.database_url` via stdlib `urllib.parse.quote` | `PROOF012` only catches an orphan `provider_calls` row; an orphan `event_log` row for a nonexistent `action_run`/`provider_call` was still invisible. The DSN change was intended to close the same reserved-character parsing gap row 148 already closed for user/password, applied to the db name. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Reverted: row above's db-name percent-encoding. `RuntimeIdentity.database_url` no longer quotes the db name (user/password quoting is unchanged) | Sixteenth-round review (verified live via `sqlalchemy.engine.make_url()` + dialect `create_connect_args()`, not just string-level DSN inspection): unlike `URL.username`/`URL.password`, which `make_url()` percent-decodes, `URL.database` (the path segment) is left exactly as found after the last `/` -- so quoting it made `_build_engine()`'s only real consumer path hand psycopg a still-encoded, wrong db name (`"mydb%23frag"` instead of `"mydb#frag"`), trading one silent-failure mode for another instead of fixing it. Leaving it unquoted round-trips correctly for every reserved character `make_url()` itself doesn't already treat as a DSN delimiter (confirmed for `#`, space, `@`, `:`, `/`, `%`); added `test_database_url_survives_the_real_sqlalchemy_consumer_path_for_reserved_db_name_chars`, which exercises the real consumer path instead of `urlsplit()`/`unquote()`. Also fixed `PROOF014`/`PROOF015`'s `sorted()` over the orphan-id set, which could raise `TypeError` instead of failing cleanly when an orphan row's `action_run_id`/`provider_call_id` is `NULL` (both are nullable `event_log` columns) alongside another orphan row with a string id; extracted the shared `_orphan_ids()` helper (None-safe sort) used by both PROOF014 and PROOF015 rather than fixing the sort in two near-duplicate blocks. | Push the fixed commit and merge PR #82. |
| 2026-08-20 | Re-added: `RuntimeIdentity.database_url` percent-encodes the db-name segment again; `_build_engine()` decodes it back after `make_url()`, but only when told to via a new `decode_database_name` keyword (default `False`) -- `atoms_proof.py`'s `--database-url-is-percent-encoded` CLI flag sets it for the real `runner.py`-driven path | Seventeenth-round review: `_build_engine()`'s only real consumer at the time was `RuntimeIdentity.database_url`'s own encoded output, so an unconditional decode was safe then; eighteenth-round review found `_build_engine()` is also a general, independently-invocable interface (`--database-url-env` + direct unit tests with hand-written DSNs), so a blind, unconditional `unquote()` could silently corrupt a database name from any other source that legitimately contains a `%`-looking substring not meant as encoding. Made the decode an explicit, caller-declared contract instead of a guess. |
| 2026-08-20 | Eighteenth code review pass (4 findings, each independently verified by executing real code): (1) `_build_engine()`'s decode made caller-declared (see row above); (2) `storage/db.py`'s `build_postgres_url_from_env()` -- the production DSN builder for platform-api/platform-worker's boot path, cited by `atoms_proof.py`'s own docstring as the pattern being matched -- now percent-encodes its database segment too, closing the same `?`/`#` query-string-injection gap row 151 closed for the dev/CLI path; `create_sync_engine()` gained the matching `decode_database_name` keyword, threaded through `bootstrap.py`'s `_resolve_database_url()` (now returns `(url, decode_database_name)`) and `platform-worker`'s `WorkerSettings`/`build_worker()` -- `migrate.py`'s Alembic path is the one caller that can't use the decode (Alembic builds its own engine internally from a plain config string with no hook to inject a decode step), so it's left consistently percent-encoded end-to-end, which still closes the silent-injection gap even though it can't resolve to the literal db name for that one path; (3) `RuntimeIdentity.database_url`'s comment claimed `URL.create()` percent-encodes user/password automatically -- true for every RFC 3986 sub-delim except a literal space, which it silently leaves unescaped; verified `URL.create()` also double-encodes if handed an already-`quote()`-d value (its own internal encoder re-escapes a literal `%`), so the property now builds the whole DSN by hand with one `quote(..., safe="")` pass per component instead of mixing manual quoting with `URL.create()`'s own; (4) `tests/test_runner.py`'s new consumer-path test loaded `atoms_proof.py` via a second, independently-cached `load_cached_module()` call instead of reusing `tests/test_atoms_proof.py`'s `load_atoms_proof_module()` -- switched to the shared helper. Added regression tests for (1)-(3) through the real `sqlalchemy.engine.make_url()` -> dialect `create_connect_args()` consumer path (not string parsing), including a `build_postgres_url_from_env()` version in `test_database_url.py`, a `WorkerSettings`/`bootstrap._build_storage_dependencies` decode-flag-routing test each, and a `migrate.py` test documenting the percent-encoded-end-to-end fallback. Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (826 passed, +4 new tests), and a live `dev-up`/`atoms-proof`/`dev-down` cycle (11/11 + 3/3, exit 0). | Push the fixed commit and merge PR #82. |
| 2026-08-21 | Nineteenth round: user-supplied inline review comments on the eighteenth-round diff (treated as untrusted data, verified against current code before fixing). Both confirmed and fixed: (1) `storage/db.py`'s `create_sync_engine()` still called `unquote(url.database)` unconditionally under `decode_database_name=True` -- unlike `atoms_proof.py`'s `_build_engine()`, which the eighteenth pass had already guarded, this sibling consumer had no `url.database is not None` check, so a DSN with no database path segment would raise `TypeError` instead of connecting; (2) this doc's own "Contracts touched > Config: none" line was stale -- `atoms-proof` has taken CLI/env config surface (`--database-url-env`, `--database-url-is-percent-encoded`, `ANYTOOLAI_ATOMS_PROOF_DATABASE_URL`) since the fourteenth round. Added `test_create_sync_engine_with_decode_flag_tolerates_a_dsn_with_no_database_segment` (exercises the real consumer path, matching this file's existing convention). Re-ran `validate-configs`/`validate-architecture`/`validate-docs`, `quick-check` (829 passed, +1 new test), and a live `dev-up`/`atoms-proof`/`dev-down` cycle (11/11 + 3/3, exit 0), all green. | Push the fixed commit and merge PR #82. |

## Open questions

None outstanding.

## Follow-up debt

None identified.
